Verifier que la pos du joueur ne soit pas entoure d'espace sinon pars_error
Implementer algo flood fill en iteratif (p-e besoin d'implementer Queue)

\\ ATTENTION TOUTES LES LIGNES NE FONT PAS LA MEME TAILLE //
\\ CHECK TAILLE DE LA LIGNE AVANT DE DESCENDRE OU DE MONTER //

limites conditions:
pour haut --> if (hauteur - 1 < 1) //error
pour bas --> if (hauteur + 1 > lstsize) //error
pour gauche --> if (index - 1 < 0) //error
pour droite --> if (index + 1 == strlen) //error
